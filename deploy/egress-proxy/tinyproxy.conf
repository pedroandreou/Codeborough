# Default-deny forward proxy. The ONLY destinations allowed off the box are those
# matched by /etc/tinyproxy/filter (ElevenLabs). Everything else gets a 403.
Port 8888
Listen 0.0.0.0
Timeout 600
LogLevel Info
Filter "/etc/tinyproxy/filter"
FilterDefaultDeny Yes
FilterExtended On
ConnectPort 443
